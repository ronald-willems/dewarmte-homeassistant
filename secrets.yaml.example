# Copy to secrets.yaml in the repository root (gitignored).
# Used by scripts/deploy-homeassistant.sh only.

ha_samba_username: "your_samba_user"
ha_samba_password: "your_samba_password"
# Long-lived access token (Profile → Security)
ha_api_token: "your_long_lived_token"
